package APNA_COLLEGE.Sorting;

/**
 * @author Shailesh
 */
public class InbuiltSorting {

    // Inbuilt sorting is sorting using java in built method or predefined methods like Arrays.sort(arr);
    // Arrays.sort(arr, si, ei) - Index based sorting if we want to sort array from particuler element we need to pass
    // Start index as SI and Last index as EI array will be sorted from the index. it is present Import java.util.Ayyars
    // Import java.util.*; - it means it will import all the functions present in util package.
    // o (n log)

    // Collection sorting also sorting in built method in Collection framework.
    //Import java.util.Collections
    // Arrays.sort(arr, Collections.reverseOrder());
    // Arrays.sort(arr, si, ei, Collections.reverseOrder());


    // **** with Collections.reverseOrder() we can't use premitive data types like int we need to use object type as Interger insead of Int.

    public static void main(String[] args) {

    }
}
